# focus-releases

Публичный канал релизов приложения **Focus** (код — в приватном репо).

- `appcast.xml` — фид Sparkle: приложение проверяет его и ставит обновления.
- DMG-сборки лежат в [Releases](../../releases).

Файлы обновляются автоматически из CI приватного репозитория по git-тегу `v*`.
